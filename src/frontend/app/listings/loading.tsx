// import { ListingsGridSkeleton } from '@/app/ui/skeletons';

// Skeleton disabled — the brief flash between searches is more jarring
// than a clean empty state. Listings cards have their own fade-up entrance.
export default function Loading() {
  return null;
}